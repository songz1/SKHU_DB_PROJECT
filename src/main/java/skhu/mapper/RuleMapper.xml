<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="skhu.mapper.RuleMapper">
	<select id="findByName" resultType="Rule">
		select * from rule where name = #{ rule }
	</select>
	<insert id="insert">
		insert into rule(name, content) values(#{ name }, #{ content })
	</insert>
	<update id="update">
		update rule
		set content = #{ content }
		where id = #{ id }
	</update>
</mapper>